language: ruby
sudo: false
cache: bundler

rvm:
  - 2.2.0
  - 2.1.0
  - 2.0.0
  - 1.9.3

before_install:
  - gem uninstall bundler
  - gem install bundler --version '~> 1.10'

script:
  - bundle exec rake

notifications:
  email:
    on_success: change
    on_failure: always
